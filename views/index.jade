doctype html
html(lang="ja")
  head
    meta(charset='utf-8')
    title= title
    meta(name='description' content='#{description}')
    meta(name='author' content='#{author}')
  body
    h1= title
    h2 Recented Posts
    - var posts = post;
    ul
    - each apost in posts
      li
        h2
          a(href="/#{apost.slug}/") #{apost.title}